@if (Session["UserID"] != null && (Session["UserRole"].Equals(Sklep.Models.DbModels.RoleType.Producer.ToString()) || Session["UserRole"].Equals(Sklep.Models.DbModels.RoleType.Admin.ToString())))
{
    using (Html.BeginForm("Producer Panel", "Account", FormMethod.Post, new { id = "producerForm", @class = "navbar-right" }))
    {
        
    }
}
